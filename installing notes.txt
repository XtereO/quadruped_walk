pip install <lib_name> -i https://mirrors.aliyun.com/pypi/simple/ --trusted-host mirrors.aliyun.com (if python does not want to load by default)
the ref is taken from Unitree A1 https://github.com/google-deepmind/mujoco_menagerie/tree/main (in unitree_a1 dir)